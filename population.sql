-- Insert rows into table 'Plane'
INSERT INTO Plane (plane_id)  -- columns to insert data into

VALUES
    (1),  -- first row: values for the columns in the list above
    (2), -- second row: values for the columns in the list above
    (3);
-- add more rows here

